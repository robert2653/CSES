#include <bits/stdc++.h>
using namespace std;
int main(){
    int n; cin >> n;
    map<int, int> mp;
    while(n--){
        int tmp; cin >> tmp;
        mp[tmp];
    }
    cout << mp.size();
}